# Everything here belongs to the repository, not to the plugin. The deploy
# workflow copies the repository into SVN trunk, so anything left out of this
# list ends up in what people download.

.git
.github
.gitignore
.distignore
.wordpress-org
tests
node_modules
vendor
composer.json
composer.lock
phpcs.xml.dist
eslint.config.mjs
README.md

# Preview fixtures, generated by tests/gen.js
v2_*.html
